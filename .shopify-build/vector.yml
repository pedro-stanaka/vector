containers:
  default:
    build:
      from: ubuntu-latest
      type: ci

steps:
  - label: Prepare and Build Vector
    timeout: 30m
    run:
    - bash scripts/environment/bootstrap-ubuntu-20.04.sh
    - bash scripts/environment/prepare.sh
    - make package-x86_64-unknown-linux-gnu-all
